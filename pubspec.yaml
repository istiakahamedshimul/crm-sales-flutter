name: real_estate_crm_sales
description: "MVP sales executive app for the real estate CRM."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  intl: ^0.19.0
  file_picker: ^8.1.7
  shared_preferences: ^2.3.2
  onesignal_flutter: 5.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
